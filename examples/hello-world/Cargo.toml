[package]
name = "sentient-hello-world"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
sentient_core = { path = "../../crates/sentient_core" }
sentient_gateway = { path = "../../crates/sentient_gateway" }
