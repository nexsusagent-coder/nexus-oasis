[package]
name = "sentient-setup"
version = "4.0.0"
edition = "2021"
description = "SENTIENT Setup Wizard v4.0.0 - Universal Omni-Gateway & Full Channel Support"
authors = ["SENTIENT Core Team"]

[dependencies]
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

# Modern TUI Libraries
dialoguer = { version = "0.11", features = ["fuzzy-select"] }
inquire = "0.7"
console = "0.15"
indicatif = "0.17"

# Config & Utils
dirs = "5.0"
toml = "0.8"
anyhow = { workspace = true }

# Async runtime for progress bars
futures = "0.3"
